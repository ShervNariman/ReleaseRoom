import { describe, expect, it } from "vitest";
import { hmacHex, isFreshTimestamp, verifyGitHubSignature, verifyLinearSignature, verifyVercelSignature } from "@/lib/integrations/providers/security";
describe("provider webhook security", () => { const body = JSON.stringify({ event: "ok" }); const secret = "provider-secret-at-least-16"; it("validates GitHub sha256 signatures", () => expect(verifyGitHubSignature(body, `sha256=${hmacHex(body, secret)}`, secret)).toBe(true)); it("rejects changed GitHub payloads", () => expect(verifyGitHubSignature(`${body}x`, `sha256=${hmacHex(body, secret)}`, secret)).toBe(false)); it("validates Linear hex signatures", () => expect(verifyLinearSignature(body, hmacHex(body, secret), secret)).toBe(true)); it("validates Vercel shared-secret signatures", () => expect(verifyVercelSignature(secret, secret)).toBe(true)); it("rejects stale Linear timestamps", () => expect(isFreshTimestamp(Date.now() - 10 * 60 * 1000)).toBe(false)); });
