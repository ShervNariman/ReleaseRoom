"use client";
import { useActionState } from "react";
import { KeyRound, ScrollText } from "lucide-react";
import { loginAction } from "./actions";
export default function LoginPage() { const [state, action, pending] = useActionState(loginAction, { error: "" }); return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#ede9fe,transparent_38%)] p-6"><div className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-8 shadow-xl shadow-slate-200/60"><span className="grid size-12 place-items-center rounded-2xl bg-slate-950 text-white"><ScrollText className="size-6"/></span><p className="mt-6 text-sm font-semibold text-violet-700">PRIVATE WORKSPACE</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Enter Release Room</h1><p className="mt-3 text-sm leading-6 text-slate-600">One calm place to decide whether a startup feature is genuinely ready for customers.</p><form action={action} className="mt-8 space-y-3"><label className="text-sm font-semibold text-slate-800" htmlFor="accessKey">Access key</label><div className="relative"><KeyRound className="absolute left-3 top-3 size-4 text-slate-400"/><input id="accessKey" name="accessKey" type="password" required className="field pl-10" placeholder="Private access key"/></div>{state.error && <p className="text-sm text-red-700">{state.error}</p>}<button type="submit" disabled={pending} className="button-primary w-full">{pending ? "Opening…" : "Open workspace"}</button></form><p className="mt-5 text-xs leading-5 text-slate-500">Local demo key: <code className="rounded bg-slate-100 px-1.5 py-1">release-room-private</code>. Set a strong environment key before deployment.</p></div></main>; }
