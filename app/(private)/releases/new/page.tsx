"use client";
import { useActionState } from "react";
import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { createReleaseAction } from "./actions";
export default function NewReleasePage(){ const [state,action,pending]=useActionState(createReleaseAction,{error:""}); return <div className="mx-auto max-w-3xl"><Link href="/releases" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-600"><ArrowLeft className="size-4"/>Releases</Link><p className="mt-8 text-sm font-semibold text-violet-700">NEW RELEASE ROOM</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Turn a change into a decision</h1><p className="mt-2 text-sm leading-6 text-slate-600">Release Room will inspect changed files, assign the right risk policy, and show the proof required before production.</p><form action={action} className="mt-8 grid gap-5 rounded-3xl border border-slate-200 bg-white p-6"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Title<input className="field mt-2" name="title" required placeholder="Team billing settings"/></label><label className="text-sm font-semibold">Owner<input className="field mt-2" name="owner" required defaultValue="Sherv"/></label></div><label className="text-sm font-semibold">Summary<textarea className="field mt-2 min-h-24" name="summary" required placeholder="What changes for the customer?"/></label><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Environment<input className="field mt-2" name="environment" required defaultValue="Production"/></label><label className="text-sm font-semibold">Repository<input className="field mt-2" name="repository" required placeholder="owner/repository"/></label><label className="text-sm font-semibold">Branch<input className="field mt-2" name="branch" required placeholder="feature/billing"/></label><label className="text-sm font-semibold">Commit SHA<input className="field mt-2" name="commitSha" required placeholder="7d92ae1"/></label></div><div className="grid gap-4 sm:grid-cols-3"><label className="text-sm font-semibold">PR URL<input className="field mt-2" type="url" name="prUrl"/></label><label className="text-sm font-semibold">Linear URL<input className="field mt-2" type="url" name="linearUrl"/></label><label className="text-sm font-semibold">Preview URL<input className="field mt-2" type="url" name="previewUrl"/></label></div><label className="text-sm font-semibold">Changed files<textarea className="field mt-2 min-h-32 font-mono text-xs" name="changedFiles" required placeholder={'app/api/stripe/route.ts\ncomponents/billing/form.tsx'}/><span className="mt-2 block text-xs font-normal text-slate-500">One path per line or comma-separated. These paths select the adaptive release policy.</span></label>{state.error&&<p className="text-sm text-red-700">{state.error}</p>}<button disabled={pending} className="button-primary">{pending?"Creating…":"Create release room"}</button></form></div>; }
