import { policyTemplates } from "@/lib/policies";
import { Card, Pill, riskTone } from "@/components/ui";
export default function PoliciesPage(){return <div><p className="text-sm font-semibold text-violet-700">POLICIES</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Adaptive proof, without process theatre</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-600">Changed files select a lightweight policy. Low-risk changes stay fast; billing, authentication, permissions, APIs, and migrations receive stronger gates.</p><div className="mt-8 grid gap-4 lg:grid-cols-2">{Object.entries(policyTemplates).map(([name,t])=><Card key={name} className="p-5"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold capitalize">{name}</h2><Pill tone={riskTone(t.level)}>{t.level} risk</Pill></div><p className="mt-2 text-sm text-slate-600">{t.reason}</p><ul className="mt-5 space-y-2">{t.requirements.map((r)=><li key={r.key} className="rounded-xl bg-slate-50 p-3"><p className="text-sm font-semibold text-slate-900">{r.label}</p><p className="mt-1 text-xs leading-5 text-slate-600">{r.description}</p></li>)}</ul></Card>)}</div></div>}
